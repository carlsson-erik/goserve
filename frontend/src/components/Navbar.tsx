import 'twin.macro'
import Button from './Button'
import { Link } from 'react-router-dom'
import paths from '../utils/paths'

const Navbar = () => {
  return (
    <div tw="bg-gray-300 flex">
      <span>Go Serve</span>
    </div>
  )
}

export default Navbar
