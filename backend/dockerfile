FROM golang:latest
WORKDIR /
COPY . .
RUN go mod download
RUN go build


# ARG DB_URL
# ENV DB_URL=$DB_URL

# ARG DB_PORT
# ENV DB_PORT=$DB_PORT

# ARG DB_USER
# ENV DB_USER=$DB_USER

# ARG DB_PASSWORD
# ENV DB_PASSWORD=$DB_PASSWORD

# ARG DB_DATABASE
# ENV DB_DATABASE=$DB_DATABASE


CMD ["./goserve"]